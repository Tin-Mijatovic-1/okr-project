/*
 * PowerPlant Messages
 *
 * This contains all the text for the PowerPlant container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PowerPlant';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PowerPlant container!',
  },
});
