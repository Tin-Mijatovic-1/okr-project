/*
 * Mine Messages
 *
 * This contains all the text for the Mine container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Mine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Mine container!',
  },
});
