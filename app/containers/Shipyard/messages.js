/*
 * Shipyard Messages
 *
 * This contains all the text for the Shipyard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Shipyard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Shipyard container!',
  },
});
