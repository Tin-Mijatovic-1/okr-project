/*
 * GameInterface Messages
 *
 * This contains all the text for the GameInterface container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GameInterface';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GameInterface container!',
  },
});
