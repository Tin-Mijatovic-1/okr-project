/*
 * Factory Messages
 *
 * This contains all the text for the Factory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Factory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Factory container!',
  },
});
